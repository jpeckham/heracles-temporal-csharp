apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-api
  namespace: heracles
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-api
  template:
    metadata:
      labels:
        app: payment-api
    spec:
      containers:
        - name: payment-api
          image: localhost:5000/heracles/payment-api:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: heracles-config
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: payment-api
  namespace: heracles
spec:
  type: NodePort
  selector:
    app: payment-api
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30081
